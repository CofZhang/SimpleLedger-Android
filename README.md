本软件由 CofZhang 完成，享有版权

# 咖啡记账 (Coffee Ledger)

一款纯本地、零联网、零广告的安卓记账 APP。所有数据保存在手机本地 SQLite 数据库，不申请任何网络权限，隐私无忧。采用暖米色 Glassmorphism 简约设计风格，集成记账、统计、日历、热力图、年账单、预算、项目、账户、桌面小组件、CSV 导入导出等丰富功能。

- 最低支持 Android 5.0（API 21）
- 目标版本 Android 13（API 33）
- 主色：`#D6C7B2`（暖米色）
- 当前版本：**v5.5.2**

---

## 一、核心记账

### 1.1 收支双向切换
- 顶部切换"支出 / 收入"两种类型
- 每笔记录可关联：分类、项目、账户、日期、备注、标签、重要标记
- 支出和收入均支持关联项目（如"旅游支出"、"工资项目"）

### 1.2 内置计算器键盘
- **禁用系统输入法**：记账页金额输入框不会弹出系统键盘，避免遮挡
- **自带数字 + 运算符键盘**：支持 `+`、`-`、`*`、`/`、`(`、`)` 表达式
- **实时计算**：顶部显示实时计算结果，如输入 `120*3+50` 显示 `410.00`
- **千分位格式化**：金额自动添加千分位分隔符（如 `1,234,567.89`）
- **光标自动修正**：表达式变化时光标位置智能保持

### 1.3 智能辅助
- **金额联想**：输入备注时自动推荐匹配分类（如输入"午餐"自动选中"餐饮"）
- **双击选分类**：双击分类区域弹出"最近使用分类"列表
- **备注小字**：账单列表中每笔账单分类名后跟浅灰色备注小字
- **重要标记**：每笔账单可标记为"重要"（⭐），列表中高亮显示

### 1.4 多标签系统
- 每笔账单可打多个标签（如 `#家庭聚餐`、`#生日`、`#出差`）
- 标签支持跨维度统计与筛选
- 在搜索框中输入关键词可同时搜索标签

---

## 二、分类管理

### 2.1 无限级嵌套
- 支持分类无限层级嵌套（如 `餐饮 → 早餐 → 路边摊 → 豆浆`）
- 面包屑导航显示当前层级路径
- 点击分类进入子分类，点击面包屑可快速返回上级

### 2.2 低饱和度莫兰迪色系
- 分类图标采用饱和度 < 30% 的莫兰迪色 + 白色线条图案
- 与暖米色主色调和谐统一，不刺眼
- 内置 30+ 常用支出分类、6+ 收入分类

### 2.3 外圈圆环选中态
- 选中时显示 2.5px `#C9B8A0` 外圈圆环
- 圆环与图标间保留 2px 间距，不遮挡原图标
- 选中时文字加粗变色，视觉反馈清晰

### 2.4 emoji 图标
- 分类使用 emoji 图标视觉区分（🍜 餐饮、🛒 购物、💰 工资 等）
- 用户可自定义分类图标和颜色

---

## 三、明细页（明细 Tab）

### 3.1 紧凑高效布局（v5.5+）
- **顶部导航栏**：更多按钮 + 月份切换 + 搜索 + 日历 + 预算按钮（44dp 单行）
- **紧凑汇总行**：当月支出 / 收入 / 结余 单行显示（12-13sp 字号）
- **账单列表**：单行紧凑布局，minHeight 44dp，一屏可见约 11 条记录
- **小图标**：分类图标 24dp 小尺寸，左对齐，文字和图标同行

### 3.2 按日期分组（鲨鱼记账风格）
- 账单按日期自动分组，每组前显示日期标题行
- 日期标题行左侧：`07月21日 Monday`
- 日期标题行右侧：当日支出小计 `-¥123.45`
- 不同日期之间有视觉间距分隔

### 3.3 月份切换
- 点击左右箭头按钮切换上/下月
- 点击月份标签弹出日期选择器，快速跳转任意月份
- 顶部汇总实时显示当前月份的支出/收入/结余

### 3.4 常驻搜索栏（v5.5.2+）
- 搜索栏常驻显示在顶部，无需点击展开
- 支持搜索：金额、分类名、账户名、标签、备注
- 输入即实时筛选，清空即恢复全部

### 3.5 长按操作菜单
- **📋 查看详情**：弹窗显示该笔账单完整信息
- **📤 分享为图片**：将账单卡片渲染为图片并分享
- **⭐ 标记为重要**：切换重要标记
- **🗑 删除**：删除该笔账单（支持 5 秒内撤销）

### 3.6 三点菜单（更多功能）
- 📊 统计：切换到统计 Tab
- 📁 项目管理
- 🗂 分类管理
- 💳 账户管理
- 📅 日历视图
- ⚙️ 设置

---

## 四、统计页（统计 Tab）

### 4.1 月度总览
- 本月总支出、总收入、结余
- 各分类支出金额 + 占比百分比 + 进度条可视化
- 颜色深浅表示金额大小

### 4.2 周/月/年视图
- **周视图**：显示本周（周一至周日）的支出分布饼状图
- **月视图**：显示本月的分类支出占比饼状图
- **年视图**：显示本年的月度收支趋势

### 4.3 分类统计列表
- 各分类按金额降序排列
- 显示金额、占比百分比、进度条
- 点击分类可查看该分类下的所有账单

---

## 五、日历视图

### 5.1 月历展示
- 月历形式显示当月每一天
- 有账单的日期下方显示小圆点，颜色深浅表示金额大小
- 左右切换上下月

### 5.2 当日详情
- 点击日期展开当日所有账单详情
- 显示当日支出/收入汇总
- 可直接编辑或删除账单

---

## 六、年账单汇总

### 6.1 顶部切换栏
- 左侧：年份选择器（带下拉箭头）
- 右侧：月账单 / 年账单切换标签

### 6.2 暖棕色汇总卡片
- 渐变背景的暖棕色卡片
- 第一行：年结余（30sp 加粗大字）
- 第二行：年收入 / 年支出（左右分栏）

### 6.3 月份列表
- 12 个月份倒序排列（12 → 1）
- 四列布局：月份 | 月收入 | 月支出 | 月结余
- 右侧灰色箭头 `>` 表示可点击查看月详情
- 表头浅灰，数据深灰，金额右对齐

---

## 七、年度热力图

### 7.1 GitHub 风格热力图
- 365 天的账单热力图，类似 GitHub 贡献热力图
- 颜色深浅表示当日支出金额
- 顶部年份选择器可切换不同年份

### 7.2 当日明细
- 点击任意日期展开当日所有账单详情
- 支持直接编辑或删除账单

---

## 八、项目管理

### 8.1 多项目支持
- 可创建多个项目（如"旅游"、"装修"、"装修"等）
- 每笔账单可关联一个项目（支出和收入均可）
- 项目列表显示该项目下的总支出和总收入

### 8.2 项目详情
- 点击项目进入详情页
- 显示该项目下所有账单记录
- 支持按时间、分类筛选

---

## 九、账户管理

### 9.1 多账户支持
- 内置账户类型：现金、银行卡、支付宝、微信
- 可自定义账户名称、图标、初始余额、备注
- 实时显示各账户当前余额

### 9.2 账户扣款
- 记账时选择扣款账户
- 支出自动扣减账户余额，收入自动增加账户余额
- 账户余额实时更新

---

## 十、预算管理

### 10.1 月度预算
- 可设置每月总预算
- 实时显示当月预算使用进度（进度条 + 百分比）
- 已用 / 剩余金额清晰显示

### 10.2 超支提醒
- 进度条颜色随使用率变化
- 超出预算时进度条变红，提示超支金额
- 明细页顶部紧凑显示预算进度条

---

## 十一、桌面小组件

### 11.1 快速记账小组件
- 窄长方形横条小组件，可放置在手机桌面
- 从左到右四个按钮：**账单** | **支出** | **收入** | **统计**
- 点击"账单" → 直接进入明细页
- 点击"支出" / "收入" → 直接开始记账（预选类型）
- 点击"统计" → 直接进入统计页

### 11.2 设计细节
- 字体大小 12sp（兼顾可读性与紧凑性）
- 与 APP 主题色统一的暖米色背景
- 4 个按钮等宽分布，间距均匀

---

## 十二、数据导入导出

### 12.1 CSV 导出
- 一键导出所有账单数据为 CSV 文件
- 使用系统 SAF（Storage Access Framework）让用户选择保存位置
- 无需申请存储权限，兼容 Android 10+ scoped storage
- CSV 文件包含 UTF-8 BOM 头，Excel 打开中文不乱码

### 12.2 CSV 导入
- 支持导入之前导出的 CSV 文件
- 自动按名称匹配分类、账户、项目
- 找不到的分类自动使用第一个分类兜底
- 方便更换设备时保留旧账单记录

---

## 十三、定时提醒

### 13.1 每日记账提醒
- 可设置每日固定时间提醒记账
- 使用 `AlarmManager` + `NotificationChannel` 本地推送
- 完全离线，不依赖任何推送服务

### 13.2 触觉反馈
- 所有点击操作触发轻微震动
- 可在设置中关闭触觉反馈
- 不同操作有不同震动强度（轻触 / 长按）

---

## 十四、设置

### 14.1 通用设置
- 触觉反馈开关
- 记账提醒时间设置

### 14.2 数据分析入口
- 年度账单热力图
- 年账单汇总

### 14.3 数据管理
- CSV 导出（含辅助说明小字）
- CSV 导入

### 14.4 关于
- 版本信息

---

## 十五、设计规范

### 15.1 色彩系统

| 用途 | 颜色 | 色值 |
|------|------|------|
| 主色 | 暖棕色 | `#D6C7B2` |
| 主色深 | 深暖棕 | `#B5A48A` |
| 背景 | 浅米白 | `#F8F3ED` |
| 背景深 | 米色 | `#EDE4D6` |
| 文字主色 | 深灰棕 | `#3D352C` |
| 文字次色 | 中灰棕 | `#8B7D6B` |
| 文字提示 | 浅灰 | `#B8AC9A` |
| 支出色 | 柔和红 | `#D4756A` |
| 收入色 | 柔和绿 | `#7BA678` |
| 结余色 | 柔和蓝 | `#8AA4B8` |

### 15.2 莫兰迪分类色系（饱和度 < 30%）

| 名称 | 色值 |
|------|------|
| 米色 | `#D6C7B2` |
| 粉色 | `#D4B5A8` |
| 蓝色 | `#A8B8C4` |
| 绿色 | `#A8B5A0` |
| 紫色 | `#B5A8B4` |
| 黄色 | `#D4C9A8` |
| 棕色 | `#B8A690` |
| 灰色 | `#B0B0B0` |

### 15.3 间距与排版
- **8 点网格系统**：所有元素间距为 8dp 的倍数（8dp、16dp、24dp、32dp）
- **文字行宽**：限制 30-38 个中文字符以保证可读性
- **顶部导航栏**：56dp 高度，矩形玻璃拟态样式
- **底部导航栏**：三列布局（明细 / 记账 / 统计）+ 凸起圆形加号按钮

### 15.4 Glassmorphism 玻璃拟态
- 20px 模糊 + 22% 白色透明
- 1px 白色内描边
- 阴影：`0 6px 24px rgba(139, 125, 107, 0.1)`

### 15.5 分类选中态规范
- 外圈圆环：2.5px `#C9B8A0` 描边
- 圆环间距：2px（不遮挡图标）
- 选中时图标颜色和图案保持不变
- 文字：主色加粗

---

## 十六、构建和安装

### 方法一：直接安装 APK（推荐普通用户）

1. 从 GitHub 仓库下载最新的 `CoffeeLedger_v5.5.2.apk`
2. 将 APK 文件传输到手机
3. 在手机文件管理器中找到 APK，点击安装
4. 首次安装需允许"未知来源应用安装"
5. 打开 APP 即可使用

> **APK 已使用开发者签名证书签名**（v1 JAR + v2 APK Signature Scheme），可直接安装。

### 方法二：使用 Android Studio

1. 下载安装 Android Studio（含 Android SDK API Level 33）
2. 打开项目根目录
3. 等待 Gradle 同步完成（已配置阿里云镜像加速）
4. 手机开启"开发者选项"和"USB 调试"，用 USB 连接电脑
5. 点击绿色运行按钮（▶️）即可编译安装到手机

### 方法三：命令行构建

1. 安装 JDK 17（https://adoptium.net/）
2. 配置 `JAVA_HOME` 环境变量
3. 创建 `local.properties`：
   ```
   sdk.dir=C\:\\Users\\你的用户名\\AppData\\Local\\Android\\Sdk
   ```
4. 运行构建：
   ```powershell
   .\gradlew.bat assembleRelease
   ```
5. APK 位置：
   ```
   app\build\outputs\apk\release\app-release.apk
   ```
6. 安装到手机：
   ```powershell
   adb install app\build\outputs\apk\release\app-release.apk
   ```

---

## 十七、技术架构

### 技术栈
- **最低支持**：Android 5.0 (API Level 21)
- **目标版本**：Android 13 (API Level 33)
- **开发语言**：Java
- **数据存储**：SQLite 本地数据库（DatabaseHelper v5）
- **UI 框架**：Material Design 1.9.0 + AndroidX AppCompat 1.6.1
- **图表库**：MPAndroidChart v3.1.0
- **网络权限**：无（完全离线）

### 项目结构

```
SimpleLedger/
├── app/
│   ├── build.gradle                    # 模块级 Gradle 配置（含签名配置）
│   └── src/main/
│       ├── AndroidManifest.xml         # 应用清单
│       ├── java/com/simpleledger/app/
│       │   ├── MainActivity.java              # 主界面（Fragment + 底部导航）
│       │   ├── RecordsFragment.java           # 明细页 Fragment
│       │   ├── AddRecordFragment.java         # 记账页 Fragment（含计算器键盘）
│       │   ├── StatsFragment.java             # 统计页 Fragment
│       │   ├── AddRecordActivity.java         # 添加记录 Activity
│       │   ├── StatsActivity.java             # 统计详情 Activity
│       │   ├── CalendarActivity.java          # 日历视图 Activity
│       │   ├── CategoryManageActivity.java    # 无限级分类管理
│       │   ├── ProjectsActivity.java          # 项目列表
│       │   ├── ProjectDetailActivity.java     # 项目详情
│       │   ├── AccountsActivity.java          # 账户管理
│       │   ├── BudgetActivity.java            # 预算管理
│       │   ├── HeatmapActivity.java           # 年度热力图
│       │   ├── PivotActivity.java             # 年账单汇总
│       │   ├── SettingsActivity.java          # 设置（CSV 导入导出）
│       │   ├── QuickWidgetProvider.java       # 桌面小组件
│       │   ├── DatabaseHelper.java            # SQLite 数据库帮助类
│       │   ├── Record/Category/Account/Project/Budget.java  # 数据模型
│       │   ├── RecordAdapter.java             # 账单列表适配器
│       │   ├── GroupedRecordAdapter.java      # 按日期分组的账单适配器
│       │   ├── CalculatorKeyboard.java        # 内置计算器键盘
│       │   ├── ExpressionEvaluator.java       # 表达式求值器
│       │   ├── HeatmapView.java               # 热力图自定义 View
│       │   ├── HapticHelper.java              # 触觉反馈工具
│       │   └── ReminderReceiver.java          # 定时提醒 BroadcastReceiver
│       └── res/
│           ├── layout/                        # 布局文件（20+ 个）
│           ├── values/                        # 字符串、颜色、主题
│           ├── drawable/                      # 图片资源
│           └── xml/quick_widget_info.xml      # 小组件配置
│
├── build.gradle                             # 项目级 Gradle 配置
├── settings.gradle                          # Gradle 设置
├── gradle.properties                        # Gradle 属性
└── README.md                                # 本文档
```

### 核心技术实现

#### 沉浸式状态栏
```java
WindowCompat.setDecorFitsSystemWindows(getWindow(), false);
ViewCompat.setOnApplyWindowInsetsListener(rootView, (v, insets) -> {
    int statusBarHeight = insets.getInsets(WindowInsetsCompat.Type.statusBars()).top;
    v.setPadding(0, statusBarHeight, 0, 0);
    return insets;
});
```

#### 内置计算器键盘
- 禁用系统输入法：`et.setShowSoftInputOnFocus(false)`
- 递归下降解析器实现表达式求值（支持 `+`、`-`、`*`、`/`、`(`、`)`）
- 实时千分位格式化，光标位置自动修正

#### 无限级分类
- 数据库 `categories` 表含 `parent_id` 字段
- `CategoryManageActivity` 递归加载子分类，面包屑导航显示层级路径

#### Storage Access Framework
- 使用 `ACTION_CREATE_DOCUMENT` / `ACTION_OPEN_DOCUMENT` 替代直接文件写入
- 解决 Android 10+ scoped storage 下的 ENOENT 错误
- 无需申请存储权限

#### 桌面小组件
- `AppWidgetProvider` + `PendingIntent` 实现按钮点击跳转
- `MainActivity` 通过 `EXTRA_TARGET` 字段区分跳转目标
- `launchMode="singleTop"` + `onNewIntent` 处理重复点击

---

## 十八、版本历史

### v5.5.2（当前版本）
- **搜索栏改为常驻显示**：明细页搜索栏不再需要点击展开，默认常驻显示
- **重写 README**：按功能模块分类重新整理文档，重点介绍各类功能

### v5.5
- **提高账单列表信息密度**：参考鲨鱼记账 APP，一屏可见约 11 条记录
- 账单条目从 ~80dp 压缩到 44dp，单行紧凑布局
- 分类图标从大圆形(48dp)缩小为 24dp 小图标，左对齐
- 顶部汇总(支出/收入/结余)整合到标题栏第二行
- 日期分组标题更紧凑（11sp 字号）
- 清理 GitHub 旧版本 APK，只保留最新 3 个

### v5.4
- 账单明细按日期分组（鲨鱼记账风格）
- 修复周视图饼状图只显示周一数据的问题
- 移除明细页三点菜单中赘余的 CSV 入口

### v5.3
- 修复 CSV 导出失败（ENOENT）：改用 SAF 框架
- 新增 CSV 账单导入功能
- 桌面小组件字体调整为 12sp

### v5.2 / v5.2.1
- 新增桌面快速记账小组件（账单 / 支出 / 收入 / 统计）
- 小组件字体大小优化

### v5.1
- 修复 5.0 版本闪退问题（`getView()` 空指针）
- 重新设计年账单汇总页面（暖棕色卡片 + 月份列表）

### v5.0
- 弹窗毛玻璃大幅增强（95% 不透明 + dimAmount 0.85）
- 明细列表添加浅灰色备注小字
- 修复分类只显示 8 个的问题

### v4.6
- 修复 APK 未签名导致安装失败
- 修复顶部三点菜单按钮被状态栏裁切
- 修复底部加号按钮被 Tab 栏遮挡

### v4.5
- 多维度透视表 / 年度账单热力图 / 内置计算器键盘
- 无限级分类嵌套 / 多标签系统
- 智能辅助 / 定时提醒 / 长按菜单

### v4.2
- 分类图标采用低饱和度莫兰迪色系
- 分类选中态改为外圈圆环

### v4.0
- Glassmorphism 玻璃拟态 UI 重设计
- 暖米色主色调 + 8 点网格间距系统

### v3.0
- 日历视图 / 账单备注 / 明细页月份选择器 / 支出支持项目选择

---

## 十九、注意事项

1. **数据本地存储**：所有数据仅保存在手机本地 SQLite 数据库，卸载 APP 会丢失数据
2. **定期备份**：建议定期使用"设置 → CSV 导出"功能备份重要数据
3. **删除分类**：删除分类时，该分类下的历史记录不会被删除，但分类名可能显示异常
4. **完全离线**：APP 不申请任何网络权限，完全离线运行，保护隐私
5. **系统要求**：Android 5.0 (API 21) 及以上

---

## 二十、常见问题

**Q: 构建时提示"Java compiler is not available"怎么办？**
A: 系统缺少 JDK（仅有 JRE）。下载 Temurin JDK 17（https://adoptium.net/），并在 `gradle.properties` 中配置：
```
org.gradle.java.home=C:/路径/jdk-17
```

**Q: 构建时下载依赖很慢怎么办？**
A: 项目已配置阿里云 Maven 镜像，国内访问速度很快。如仍慢，检查网络连接。

**Q: 手机连接电脑后没反应？**
A: 确认已开启 USB 调试，并且手机上点击了"允许此电脑调试"。运行 `adb devices` 检查设备是否连接。

**Q: 安装时提示"解析包错误"？**
A: 确保手机系统版本是 Android 5.0 或以上，并且 APK 文件完整下载。

**Q: APK 安装失败提示"未签名"？**
A: 从 v4.6 起，所有 release APK 均已使用开发者签名证书签名（v1 + v2 方案），可直接安装。

**Q: CSV 导出失败提示 ENOENT？**
A: v5.3 已修复此问题（改用 SAF 框架）。请升级到 v5.3 或更高版本。

**Q: 闪退怎么办？**
A: v5.0 版本存在已知闪退问题（已在 v5.1 修复）。请升级到最新版本。

---

## 版权声明

**本软件由 CofZhang 完成，享有版权。**

© 2026 CofZhang. All rights reserved.

未经授权，禁止复制、传播、修改或用于商业用途。
